<!--
  Add these <uses-permission> entries inside <manifest> in:
  android/app/src/main/AndroidManifest.xml

  Run after: npx cap add android
  Most are auto-added by Capacitor plugins, but listed here for clarity
  and Play Store data-safety disclosure.
-->

<!-- Internet & network state (auto-added by Capacitor core) -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<!-- Microphone for voice-to-text -->
<uses-permission android:name="android.permission.RECORD_AUDIO" />

<!-- Camera for profile photo capture -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" android:required="false" />

<!-- Photo library / media access (Android 13+ uses scoped media perms) -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission
  android:name="android.permission.READ_EXTERNAL_STORAGE"
  android:maxSdkVersion="32" />

<!-- Push & local notifications -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />